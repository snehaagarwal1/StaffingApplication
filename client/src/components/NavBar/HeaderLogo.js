import React from "react";
import logo from "../../assets/staffingapplication-logo.svg"
const HeaderLogo = ({ onClick }) => {
  return (
    <div onClick={onClick} >
      chvasbbvnan
      {/* <img src={logo} style={{width: "300px",paddingTop: "5px"}} /> */}
    </div>
  );
};

export default HeaderLogo;
